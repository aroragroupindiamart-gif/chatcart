# WAStore Builder — Verify Pro Tier Features Are Actually Built + Add Mandatory "All Items" Category

## TASK 1 — Audit: confirm every advertised Pro-tier feature actually works

Before this pricing page goes live to real sellers, verify each Pro-tier line item against real working code. For EACH item below, confirm Done / Partial / Not Built, with proof (not just a claim):

1. Unlimited products — confirm no cap is applied for Pro-tier sellers.
2. Custom store branding (logo + tagline) — confirm this works for Pro and is blocked for Starter/Growth (per the most recent plan tier prompt).
3. Bulk CSV import — confirm this is actually built and functional, not just gated/planned. If it was never built (earlier audits suggested it wasn't), say so clearly — do not advertise it as working if it isn't.
4. Monthly store data export — confirm this was actually implemented (it was specified as new in a recent prompt) — show it working with a real export.
5. Up to 3 staff logins — confirm multi-staff-login functionality actually exists at all. Earlier audits suggested only single-login (one phone number per seller account) was ever built — if multi-staff login does not exist as a real feature, flag this clearly as NOT BUILT, since this cannot be advertised if it doesn't exist.
6. WhatsApp + phone support, 24/7 instant response — this is an operational commitment (you/your team answering messages), not something to verify in code. No proof needed here, just confirmed as understood to be operational, not technical.

## What to do based on findings
- For anything confirmed "Not Built" or "Partial," do NOT remove it from the pricing page yet — instead, report back clearly what's missing so a decision can be made (build it now, or temporarily adjust the pricing page copy) before any real seller is sold a Pro plan based on a feature that doesn't exist.

## TASK 2 — Add a mandatory "All Items" category to every storefront

1. On every seller's customer-facing storefront, add a permanent, always-present "All" category/tab that is NOT a real seller-created category — it's a built-in view showing every active product across all of that seller's categories combined.
2. This should appear first/by default when a customer lands on the storefront (i.e. "All" is the default view, with the seller's real categories as additional tabs/sections alongside or below it).
3. This applies regardless of how many real categories a seller has set up — even a seller with just one category should still have "All" available (in that case it would look identical to their one category, which is fine).

## Proof required
1. The full Task 1 audit table (feature, status, evidence) — be completely honest, especially about multi-staff login and bulk CSV import, since these are the two most likely to be unbuilt based on earlier audits.
2. Screenshot of a storefront showing the "All" tab/category alongside real categories, and confirm it correctly shows every active product when selected.
